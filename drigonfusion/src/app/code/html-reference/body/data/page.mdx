import { Separator } from "~/components/ui/separator";

# data
<Separator />
The `data` element is used to define data or information. It is typically used to indicate the data or information that is being provided.

Example:
```html
<data value="42">The answer to the ultimate question of life, the universe, and everything is 42.</data>
```