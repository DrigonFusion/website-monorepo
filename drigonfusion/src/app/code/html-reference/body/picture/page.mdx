import { Separator } from "~/components/ui/separator";

# picture
<Separator />
The `picture` element is used to define a picture. It is typically used to indicate a picture that is being used.

Example:
```html
<picture>
  <source srcset="image.jpg" type="image/jpeg">
  <img src="image.jpg" alt="An image">
</picture>
```