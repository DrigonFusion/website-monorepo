import { Separator } from "~/components/ui/separator";

# dfn
<Separator />
The `dfn` element is used to define a definition term. It is typically used to indicate a term that is defined elsewhere in the document.

Example:
```html
<dfn>This is a definition term.</dfn>
```