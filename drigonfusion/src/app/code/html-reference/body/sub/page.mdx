import { Separator } from "~/components/ui/separator";

# sub
<Separator />
The `sub` element is used to define subscript text. It is typically used to indicate the subscript text that is being used.

Example:
```html
CO<sub>2</sub>
```