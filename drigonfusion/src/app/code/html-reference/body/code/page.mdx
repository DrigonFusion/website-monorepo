import { Separator } from "~/components/ui/separator";

# code
<Separator />
The `code` element is used to define computer code or programming language. It is typically used to indicate the code or programming language that is being used.

Example:
```html
<code>const x = 42;</code>
```