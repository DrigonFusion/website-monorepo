import { Separator } from "~/components/ui/separator";

# cite
<Separator />
The `cite` element is used to define the source of a quote or a reference. It is typically used to indicate the author, title, or other information about the source of a quote or reference.

Example:
```html
<cite>This quote is from a book by John Smith.</cite>
```