import { Separator } from "~/components/ui/separator";

# s
<Separator />
The `s` element is used to define strikethrough text. It is typically used to indicate that text has been struck out or deleted.

Example:
```html
<s>This text has been struck out.</s>
```