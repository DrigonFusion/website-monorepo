import { Separator } from "~/components/ui/separator";

# mark
<Separator />
The `mark` element is used to define marked or highlighted text. It is typically used to indicate the marked or highlighted text that is being used.

Example:
```html
<mark>This text is marked or highlighted.</mark>
```