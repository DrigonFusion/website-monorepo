import { Separator } from "~/components/ui/separator";

# strong
<Separator />
The `strong` element is used to define strong importance or importance in a text. It is typically used to emphasize or highlight text.

Example:
```html
<strong>This text is important.</strong>
```
