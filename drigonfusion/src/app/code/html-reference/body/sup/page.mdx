import { Separator } from "~/components/ui/separator";

# sup
<Separator />
The `sup` element is used to define superscript text. It is typically used to indicate the superscript text that is being used.

Example:
```html
x<sup>2</sup> + y<sup>2</sup> = 5<sup>2</sup>
```