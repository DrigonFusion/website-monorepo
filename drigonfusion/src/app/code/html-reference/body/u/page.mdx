import { Separator } from "~/components/ui/separator";

# u
<Separator />
The `u` element is used to define underlined text. It is typically used to indicate the underlined text that is being used.

Example:
```html
<u>This text is underlined.</u>
```