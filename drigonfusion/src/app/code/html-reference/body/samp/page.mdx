import { Separator } from "~/components/ui/separator";

# samp
<Separator />
The `samp` element is used to define sample output from a computer program. It is typically used to indicate the output of a program or the output of a computer program.

Example:
```html
<samp>This is sample output from a computer program.</samp>
```