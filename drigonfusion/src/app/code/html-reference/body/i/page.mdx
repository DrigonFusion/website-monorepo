import { Separator } from "~/components/ui/separator";

# i
<Separator />
The `i` element is used to define italic text. It is typically used to indicate the italic text that is being used.

Example:
```html
<i>This text is italic.</i>
```