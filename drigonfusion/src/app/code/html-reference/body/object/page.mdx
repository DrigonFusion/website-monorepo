import { Separator } from "~/components/ui/separator";

# object
<Separator />
The `object` element is used to define an object. It is typically used to indicate an object that is being used.

Example:
```html
<object data="image.jpg" type="image/jpeg"></object>
```