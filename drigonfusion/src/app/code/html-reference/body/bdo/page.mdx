import { Separator } from "~/components/ui/separator";

# bdo
<Separator />
The `bdo` element is used to override the directionality of text. It is typically used to indicate the directionality of text that is being used.

Example:
```html
<bdo dir="rtl">This text will be displayed right-to-left.</bdo>
```