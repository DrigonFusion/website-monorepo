import { Separator } from "~/components/ui/separator";

# del
<Separator />
The `del` element is used to define a deleted text. It is typically used to indicate a deleted text that is being used.

Example:
```html
<del>This is a deleted text.</del>
```