import { Separator } from "~/components/ui/separator";

# em
<Separator />
The `em` element is used to define emphasized text. It can be used to indicate that text is important or to highlight key words.

Example:
```html
<em>This is emphasized text.</em>
```
