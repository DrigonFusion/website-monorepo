import { Separator } from "~/components/ui/separator";

# abbr
<Separator />
The `abbr` element is used to define an abbreviation or acronym. It is typically used to indicate a short form of a word or phrase.


Example:
```html
<abbr title="World Wide Web">WWW</abbr>
```