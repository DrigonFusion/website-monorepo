import { Separator } from "~/components/ui/separator";

# q
<Separator />
The `q` element is used to define a short inline quotation. It is typically used to indicate a quote or a reference.

Example:
```html
<q>This is a short quotation.</q>
```