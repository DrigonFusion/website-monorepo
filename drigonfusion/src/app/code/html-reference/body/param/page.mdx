import { Separator } from "~/components/ui/separator";

# param
<Separator />
The `param` element is used to define a parameter. It is typically used to indicate a parameter that is being used.

Example:
```html
<object data="rr.wav">
  <param name="autoplay" value="true">
</object>
```