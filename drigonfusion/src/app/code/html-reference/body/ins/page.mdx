import { Separator } from "~/components/ui/separator";

# ins
<Separator />
The `ins` element is used to define an inserted text. It is typically used to indicate an inserted text that is being used.

Example:
```html
<ins>This is an inserted text.</ins>
```