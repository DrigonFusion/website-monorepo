import { Separator } from "~/components/ui/separator";

# p
<Separator />
The `p` element is used to define a paragraph. It can be used to group related text or to provide a description of a term or a concept.

Example:
```html
<p>This is a paragraph of text.</p>
```
