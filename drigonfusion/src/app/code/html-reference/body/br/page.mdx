import { Separator } from "~/components/ui/separator";

# br
<Separator />
The `br` element is used to define a line break. It is typically used to indicate a line break that is being used.

Example:
```html
<p>This is a paragraph before a line break.</p>
<br>
<p>This is another paragraph after a line break.</p>
```