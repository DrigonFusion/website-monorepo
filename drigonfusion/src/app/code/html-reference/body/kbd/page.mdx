import { Separator } from "~/components/ui/separator";

# kbd
<Separator />
The `kbd` element is used to define keyboard input. It is typically used to indicate the keyboard input that is being used.

Example:
```html
<kbd>Ctrl + C</kbd>
```