import { Separator } from "~/components/ui/separator";

# small
<Separator />
The `small` element is used to define small size or size in a text. It is typically used to reduce the size of text.

Example:
```html
<small>This text is small.</small>
```
