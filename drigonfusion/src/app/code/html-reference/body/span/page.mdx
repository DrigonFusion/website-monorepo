import { Separator } from "~/components/ui/separator";

# span
<Separator />
The `span` element is used to define a span of text. It is typically used to indicate a span of text that is being used.

Example:
```html
<span>This is a span of text.</span>
```