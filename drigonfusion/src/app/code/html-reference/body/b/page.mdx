import { Separator } from "~/components/ui/separator";

# b
<Separator />
The `b` element is used to define bold text. It is typically used to indicate the bold text that is being used.

Example:
```html
<b>This text is bold.</b>
```