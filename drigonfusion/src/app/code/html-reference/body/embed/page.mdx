import { Separator } from "~/components/ui/separator";

# embed
<Separator />
The `embed` element is used to define an embedded object. It is typically used to indicate an embedded object that is being used.

Example:
```html
<embed src="video.mp4" type="video/mp4">
```