import { Separator } from "~/components/ui/separator";

# bdi
<Separator />
The `bdi` element is used to define bidirectional text. It is typically used to indicate the bidirectional text that is being used.

Example:
```html
<bdi>This text is bidirectional.</bdi>
```