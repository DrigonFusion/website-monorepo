import { Separator } from "~/components/ui/separator";

# title
<Separator />
The `title` element is used to define the title of the document, which is displayed in a browser's title bar or tab.

Example:
```html
<title>My Web Page</title>
```
